# Plain Kustomize file. Just the one ServiceMonitor for now; kept as its own layer for the
# dependsOn reasons explained in observability-config.yaml one level up.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - demo-app-servicemonitor.yaml
